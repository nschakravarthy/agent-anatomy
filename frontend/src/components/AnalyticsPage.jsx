// Admin-only tab. See ConfigurationPage for how the gate is enforced.
export default function AnalyticsPage() {
  return (
    <section className="placeholder-page">
      <h2>Analytics</h2>
      <p>Conversation and usage metrics live here. Visible to admins only.</p>
    </section>
  );
}
